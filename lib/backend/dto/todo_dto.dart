class TodoDto {
  final String title;
  final String description;
  final bool isCompleted;

  const TodoDto(
      {required this.title,
      required this.description,
      required this.isCompleted});
}
